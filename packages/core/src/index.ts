// @cms-core — entrypoint público do pacote core.
//
// Fase 0: contrato ClientConfig + validador Zod (S0.1) e builders de
// equivalência gold-standard (S0.3). As demais capabilities (engine, auth,
// media, codegen completo) chegam nas fases seguintes.

export * from "./config/index.js";
