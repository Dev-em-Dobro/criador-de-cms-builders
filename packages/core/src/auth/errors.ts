// @cms-core/core/auth — re-export das classes de erro compartilhadas.
// Fonte única: ../errors.ts (ver nota lá sobre `instanceof`).
export { AuthError } from "../errors.js";
export type { AuthNext } from "../errors.js";
